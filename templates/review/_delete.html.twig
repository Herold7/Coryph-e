
<form method="post" action="{{ path('remove_review', {'id': review.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet avis ?');">
    <input type="hidden" name="_csrf_token" value="{{ csrf_token('remove-review') }}">
    <button class="btn btn-danger">supprimer cet avis</button>
</form>

