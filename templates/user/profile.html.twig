<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Completer votre profil</title>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@900&family=Poppins:wght@300;500&display=swap" rel="stylesheet">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
		<link rel="stylesheet" href="">
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous" defer></script>
	</head>
	<body class="bg-body-tertiary">
		<div class="vh-100 d-flex flex-column align-items-center justify-content-center">
			<img src="{{asset('/images/icon.svg')}}" alt="BnB logo" width="200">
			<h1 class="text-center my-4">Completer votre profil</h1>
            <p>
                Juste quelques étapes supplémentaires pour compléter votre profil et commencer à utiliser Coryphée.
            </p>
			</p>
			{{ form_start(form, {
				'attr': {
					'class': 'col-md-6 col-sm-10'}
				}
			) }}
			{{ form_errors(form) }}
			<div class="form-floating mb-2">
				{{ form_widget(form.roles, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.roles
						}) }}
				<label for="floatingInput">What kind of user are you?</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.name, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.name
						}) }}
				<label for="floatingInput">Votre nom</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.corporateName, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.corporateName
						}) }}
				<label for="floatingInput">Raison social</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.siret, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.siret
						}) }}
				<label for="floatingInput">Votre numéro de siret</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.phone, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.phone
						}) }}
				<label for="floatingInput">Votre numéro de téléphone</label>
			</div>
            <div class="form-floating mb-2">
				{{ form_widget(form.address, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.address
						}) }}
				<label for="floatingInput">Votre adresse</label>
			</div>
            <div class="form-floating mb-2">
				{{ form_widget(form.additionalAddress, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.additionalAddress
						}) }}
				<label for="floatingInput">Complément d'adresse</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.city, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.city
						}) }}
				<label for="floatingInput">Votre ville</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.zip, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.zip
						}) }}
				<label for="floatingInput">Votre code postal</label>
			</div>
			<div class="form-floating mb-2">
				{{ form_widget(form.country, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.country
						}) }}
				<label for="floatingInput">Votre pays</label>
			</div>
			<div class="input-group mb-2">
				{{ form_widget(form.image, {
							attr: {
								class: 'form-control',
							},
							'value': app.user.image
						}) }}
				<label class="input-group-text">
					<i class="bi bi-file-image"></i>
				</label>
			</div>
            <div class="form-check mb-2">
                <p>
                    En envoyant ce formulaire, vous acceptez que Coryphée mémorise et utilise les informations collectées afin de traiter votre demande. Afin d'en savoir plus, vous pouvez cliquez sur notre politique de protection des donnees personnelles.
                </p>
                {{ form_widget(form.consent, {
                    attr: {
                        class: 'form-check-input',
                    },
                    'label_attr': {
                        class: 'form-check-label',
                    },
                    'row_attr': {
                        class: 'mb-2',
                    },
                    'value': app.user.consent
                }) }}
                <label class="form-check-label" for="{{ form.consent.vars.id }}">Oui, je donne mon consentement</label>
            </div>
			<button type="submit" class="mt-2 btn btn-dark btn-lg rounded-pill">
				Enregistrer
			</button>
			{{ form_end(form) }}
		</div>
	</body>
</html></body></html>
