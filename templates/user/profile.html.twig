<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Completer votre profil</title>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@900&family=Poppins:wght@300;500&display=swap" rel="stylesheet">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
		<link rel="stylesheet" href="">
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous" defer></script>
	</head>
	<body class="bg-body-tertiary">
		<div class="vh-100 d-flex flex-column align-items-center justify-content-center" style="margin: 10rem 0 10rem 0;">
			<img src="{{ asset('/images/logo/logo-noir2.jpeg') }}" alt="Coryphee logo" width="100">
			<h1 class="text-center my-4">Completer votre profil</h1>
			{% if app.user %}
            <p>
                Juste quelques étapes supplémentaires afin de compléter votre profil et commencer à utiliser Coryphée.
            </p>
				{{ form_start(form, {'attr': {'class': 'col-md-6 col-sm-10'}}) }}
				{{ form_errors(form) }}
				<div class="form-floating mb-2">
					{{ form_widget(form.name, {attr: {class: 'form-control'}, 'value': app.user.name}) }}
					<label for="floatingInput">Votre nom *</label>
					<div class="form-error">{{ form_errors(form.name) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.corporateName, {attr: {class: 'form-control'}, 'required': false}) }}
					<label for="floatingInput">Raison sociale</label>
					<div class="form-error">{{ form_errors(form.corporateName) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.siret, {attr: {class: 'form-control'}, 'required': false}) }}
					<label for="floatingInput">Votre numéro de SIRET</label>
					<div class="form-error">{{ form_errors(form.siret) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.phone, {attr: {class: 'form-control'}, 'required': false}) }}
					<label for="floatingInput">Votre numéro de téléphone</label>
					<div class="form-error">{{ form_errors(form.phone) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.address, {attr: {class: 'form-control'}, 'required': false}) }}
					<label for="floatingInput">Votre adresse</label>
					<div class="form-error">{{ form_errors(form.address) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.additionalAddress, {attr: {class: 'form-control'}, 'required': false}) }}
					<label for="floatingInput">Complément d'adresse</label>
					<div class="form-error">{{ form_errors(form.additionalAddress) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.city, {attr: {class: 'form-control'}, 'value': app.user.city}) }}
					<label for="floatingInput">Votre ville *</label>
					<div class="form-error">{{ form_errors(form.city) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.zip, {attr: {class: 'form-control'}, 'value': app.user.zip}) }}
					<label for="floatingInput">Votre code postal *</label>
					<div class="form-error">{{ form_errors(form.zip) }}</div>
				</div>
				<div class="form-floating mb-2">
					{{ form_widget(form.country, {attr: {class: 'form-control'}, 'value': app.user.country, 'required': false}) }}
					<label for="floatingInput">Votre pays</label>
					<div class="form-error">{{ form_errors(form.country) }}</div>
				</div>
				<div class="input-group mb-2">
					{{ form_widget(form.image, {attr: {class: 'form-control'}, 'value': app.user.image, 'required': false}) }}
					<label class="input-group-text">
						<i class="bi bi-file-image"></i>
					</label>
					<div class="form-error">{{ form_errors(form.image) }}</div>
				</div>
				<button type="submit" class="mt-2 btn btn-dark btn-lg rounded-pill">Enregistrer</button>
				{{ form_end(form) }}
			{% else %}
				<p>Confirmer votre adresse email avant de compléter votre profil</p>
			{% endif %}
		</div>
	</body>
</html>
